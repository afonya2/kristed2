{
    shopname = "Kristed2", -- The name of the shop
    scale = 0.5, -- The monitor scale
    owner = "Bagi_Adam", -- The owner of the shop
    desc = "Hello, world!", -- The description of the shop
    redstone = "bottom", -- The way where the redstone gets emitted
    address = "", -- The krist address
    privKey = "", -- The private key of krist wallet
    selfId = "turle_", -- The turtle's network ID
    webhook = false, -- The webhook switch
    ["webhook_url"] = "", -- The url to the webhook (only discord)
    giveReceipts = false, -- If enabled the shop will give receipts after purchases
    printerId = "printer_", -- The printer's network ID
    shopsync = true, -- Enables the shopsync feature (https://p.sc3.io/7Ae4KxgzAM)
    dynamicPricing = false, -- Enables the dynamic pricing feature
    categories = { -- The categories
        {
            name = "Items", -- Some friendly display name for the category
            forcePrice = false -- If true then the dynamic pricing is not touching the price of the items in the category
        }
    },
    items = { -- The items
        {
            name = "Test", -- Some friendly display name for the item
            id = "minecraft:redstone", -- The id of the item
            price = 1, -- The price of the item in kst
            category = 1, -- The id of the category of the item
            normalStock = 10, -- The normal stock of the item Only needed if dynamic pricing is enabled
            forcePrice = false -- If true then the dynamic pricing is not touching the price of this item
        }
    }
}